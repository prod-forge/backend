name: Rollback ECS

on:
  workflow_dispatch:
    inputs:
      task_revision:
        description: 'Task definition revision (for example: my-service:23)'
        required: true

env:
  AWS_REGION: eu-central-1
  TASK_NAME: prod-forge-todolist-task
  ECS_CLUSTER: prod-forge-todolist-cluster
  ECS_SERVICE: prod-forge-todolist-service
  GITHUB_ARN: arn:aws:iam::096497994622:role/github-actions-ecs-ecr-role

permissions:
  id-token: write
  contents: read

jobs:
  rollback:
    runs-on: ubuntu-latest

    steps:
      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: ${{ env.GITHUB_ARN }}
          aws-region: eu-central-1

      - name: Rollback ECS service
        run: |
          aws ecs update-service \
            --cluster ${{ env.ECS_CLUSTER }} \
            --service ${{ env.ECS_SERVICE }} \
            --task-definition ${TASK_NAME}:${{ github.event.inputs.task_revision }} \
            --desired-count 1 \
            --force-new-deployment \
            --no-cli-pager \
            > /dev/null

      - name: Wait for deployment to finish
        run: |
          echo "Waiting for ECS deployment to stabilize..."

          aws ecs wait services-stable \
            --cluster ${{ env.ECS_CLUSTER }} \
            --services ${{ env.ECS_SERVICE }}

          echo "Deployment completed successfully!"
